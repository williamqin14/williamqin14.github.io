Just a xml generator.

## Simple fullstack calculator app

A small calculator demo was added in `calculator-app/`.

### Run it

```bash
node calculator-app/server.js
```

Then open `http://localhost:3000`.
